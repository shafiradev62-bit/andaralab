/**
 * Generate List Bug_Error complete xlsx (items 1-22, auto-fill #14-22).
 * Run: node scripts/generate-bug-list-xlsx.mjs
 */
import { createRequire } from "module";
import { fileURLToPath } from "url";
import path from "path";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const require = createRequire(path.join(__dirname, "package.json"));
const XLSX = require("xlsx");

const rows = [
  [1, "Home Page kembali ke interface awal sebelum di update (hero image, menu&submenu, statis content)", "proses deploy ke VPS sebelumnya menimpa seluruh folder frontend, termasuk hasil kustomisasi yg sudah dilakukan sebelumnya. root cause nya berasal dari docker image yang dibuild ulang dari source code lama, sehingga semua perubahan tampilan (hero image), struktur menu, dan konten statis kembali ke default. saat ini sudah diperbaiki dgn memisahkan proses deploy frontend dan backend, memastikan build menggunakan source code terbaru, serta menambahkan mekanisme backup otomatis setiap deploy sehingga rollback bisa dilakukan dalam hitungan menit jika terjadi masalah serupa.", "TRUE", ""],
  [2, "Grafik chart tidak berurut dengan tahun/bulan/Q", "data yang diinput melalui cms tidak memiliki mekanisme pengurutan otomatis, sehingga saat ditampilkan di chart, urutan data mengikuti urutan input bukan urutan kronologis. hal ini menyebabkan grafik terlihat acak dan tidak bisa dibaca dengan benar. sudah diperbaiki dengan menambahkan logika sorting di sisi backend sebelum data dikirim ke frontend. serta normalisasi format periode (tahun, bulan, kuartal) supaya konsisten dan selalu tampil berurutan dari yang terlama ke terbaru.", "TRUE", ""],
  [3, "login page u/ akses CMS hilang", "setelah satu proses deploy, route /admin tidak dapat diakses dan mengembalikan 404. investigasi menunjukkan bahwa konfigurasi Nginx untuk SPA routing tidak menangani path /admin dengan benar setelah file index.html diganti. sudah diperbaiki dengan memastikan konfigurasi Nginx selalu menggunakan try_files $uri /index.html untuk semua route, sehingga react router bisa menangani navigasi internal termasuk halaman login cms.", "TRUE", ""],
  [4, "kategori pada dataset tidak muncul semua di chart", "ditemukan bahwa komponen chart hanya merender kategori yang ada di data point pertama, sehingga kategori yang baru ditambahkan di tengah datasets tidak ikut tampil. selain itu, terdapat inkosistensi format key kategori (huruf besar/kecil, spasi yang menyebabkan data tidak ter-mapping dengan benar. diperbaiki dengan memperbaiki logika agregasi kategori di frontend agar membaca semua data point, serta menambahkan normalisasi key sebelum proses rendering chart.", "TRUE", ""],
  [5, "data failed to upload karena payload too large", "upload file csv dataset gagal dengan munculnya error 413 Payload Too Large karena batas default express.js dan nginx hanya allow request body maksimal 1mb. dataset ekonomi yang diupload bisa mencapai 10-30mb. sudah diperbaiki dengan menaikkan batas di konfigurasi express (express.json({limit: '50mb'}) dan nginx(client_max_body_size 50mb), serta menambahkan validasi ukuran file di sisi frontend agar user mendapat pesan error yang jelas sebelum proses upload dimulai", "TRUE", ""],
  [6, "updated content tidak muncul di cms dan website", "konten yang sudah disimpan di cms tidak langsung tampil di website disebabkan karena dua hal. pertama, browser melakukan cache secara agresif pada response API sehingga data lama masih ditampilkan. kedua, ada race condition antara proses simpan data dan proses fetch ulang di frontend. sudah diperbaiki dengan menambahkan header Cache-Control: no-store pada semua response API, memperbaiki mekanisme invalidasi cache di react query setiap mutasi, serta memastikan data di re-fetch otomatis setelah operasi create/update/delete berhasil", "TRUE", ""],
  [7, "Data baru tidak memunculkan grafik", "komponen InteractiveChart pada build frontend sebelumnya menggunakan versi lama yang tidak memiliki mekanisme auto-detection untuk skala Y-axis ketika data baru memiliki magnitude yang sangat berbeda dari data lama. ketika dataset baru ditambahkan dengan format kolom atau periode yang berbeda dari dataset lama, parser parseXLabel tidak dapat mengenali format tersebut sehingga data dianggap kosong dan chart menampilkan 'No data points to display'. sudah diperbaiki dengan men-deploy ulang InteractiveChart versi terbaru yang memiliki: (1) auto log-scale detection ketika rasio antar series melebihi 50x sehingga semua series tetap terlihat, (2) dynamic Y-axis scaling yang memperhitungkan padding dan tidak memaksa baseline 0 ketika range data terlalu besar, (3) improved parseXLabel yang mengenali format BPS/Indonesia (Q1 2024, Jan 2024, 2024, dll) dan melakukan sorting kronologis otomatis, serta (4) fallback ke preserveInputOrder jika format periode tidak dapat di-parse.", "TRUE", ""],
  [8, "Artikel blog yang seharunya masuk ke sectoral intelligence livenya tetap muncul di macro foundation", "komponen SectionPage yang menampilkan artikel per kategori menggunakan logika matching yang terlalu agresif. melakukan word-splitting pada nama kategori dan mencocokkan setiap kata individual. sudah diperbaiki dengan mengganti logika matching menjadi strict direct-match: post hanya muncul di section tertentu jika field category, subcategory, atau tag dari post tersebut secara langsung cocok (exact match atau contains) dengan filter kategori halaman, tanpa word-splitting yang bisa menyebabkan false positive.", "TRUE", ""],
  [9, "additional: untuk body text di artikel blog apakah bisa ditambahkan font size dan menu bold?", "sebelumnya body text artikel blog di-render sebagai plain paragraph tanpa formatting apapun. sudah ditambahkan dukungan markdown-like formatting di ArticlePage: # heading besar, ## subheading, ### sub-subheading, dan **teks** untuk bold. ditambah BlogDocEditor di CMS dengan toolbar Bold, Heading, bullet list, dan upload image seperti Google Docs.", "TRUE", ""],
  [10, "kebanyakan additional image untuk artikel blog tidak bisa diupload", "upload image gagal untuk file di atas ~1MB karena konfigurasi default Nginx membatasi request body maksimal 1MB. sudah diperbaiki dengan menambahkan client_max_body_size 50m di konfigurasi Nginx frontend container. backend validasi maksimal 10MB per file individual.", "TRUE", ""],
  [11, "posisi image pada cms tidak sesuai dengan pada saat live", "field imagePosition (top/bottom/left/right) sudah tersimpan di database CMS. sudah diperbaiki dengan conditional rendering di ArticlePage dan normalisasi imagePosition di backend saat save/load sehingga nilai bottom/left/right selalu ikut tersimpan dan di-render di posisi yang benar tanpa perlu konfigurasi manual tambahan oleh client.", "TRUE", ""],
  [12, "Suggestion pada search bar di data hub -> interactive chart tidak menunjukan kesesuaian dengan apa yang diketik di searchbar", "logika search filter diperluas ke kolom dataset, subcategory, dan alias ekonomi otomatis (GDP, PDB, Gross Domestic Product, gdp-growth, dll). suggestion diurutkan berdasarkan relevance score sehingga dataset GDP muncul paling atas saat user mengetik 'GDP' tanpa perlu mengatur apapun di CMS.", "TRUE", ""],
  [13, "Tabel pada live website kembali seperti semula, begitupun pada cms fitur custom theme table hilang", "fitur custom table theme sudah di-deploy: DatasetPreviewTable dengan dukungan tableStyle di Data Hub tab Table, dan AdminPage DatasetEditor dengan panel Table Color Palette. warna default oranye (#E67E22) bisa di-override per dataset.", "TRUE", ""],
  [14, "Artikel blog dalam bahasa inggris, muncul di live website saat dalam mode bahasa indonesia", "diperbaiki dengan strict locale filter (p.locale === locale) di DynamicPage PostsSection, FeaturedInsights, LatestInsights, dan DataHub latest research. artikel EN tidak lagi muncul saat mode ID aktif, dan sebaliknya.", "TRUE", ""],
  [15, "Ada beberapa artikel blog yang muncul di sub menu yang tidak seharusnya", "diperbaiki dengan postMatchesCategories di DynamicPage — strict match berdasarkan subcategory dan slug halaman (policy-monetary vs geopolitical), tanpa word-splitting 'macro' yang menyebabkan artikel Policy & Monetary Watch ikut muncul di Geopolitical.", "TRUE", "artikel seharusnya hanya di Policy and Monetary Watch, tidak di Geopolitical"],
  [16, "Untuk latest research di home page, judul artikel bahasa indonesia muncul saat mode bahasa inggris (dan sebaliknya)", "diperbaiki dengan filter locale ketat di komponen DataHub (latest research table), FeaturedInsights, dan LatestInsights. hanya artikel dengan locale yang sama dengan mode bahasa aktif yang ditampilkan.", "TRUE", ""],
  [17, "Button see all dan all research mengarah ke market pulse", "tombol See all, View all, dan All Research dihapus dari Featured Insights, Latest Insights, dan Data Hub snapshot karena tidak ada halaman agregat seluruh riset.", "TRUE", ""],
  [18, "Tag biru = submenu, tag merah = tag artikel", "pill biru (SUBMENU_TAG_PILL) menampilkan subcategory/submenu artikel. pill merah (ARTICLE_TAG_PILL) menampilkan field tag. diterapkan di homepage, section pages, article page, dan dynamic CMS pages.", "TRUE", ""],
  [19, "Hero image artikel muncul di Featured Insights dan Latest Insights", "hero image artikel ditampilkan di kartu Featured Insights dan Latest Insights (hero card, side cards, dan grid) serta listing artikel di section/dynamic pages.", "TRUE", ""],
  [20, "Fitur editor blog menyerupai Google Docs", "BlogDocEditor di CMS Admin dengan toolbar Bold, Heading 1-3, bullet list, insert/upload image. konten tersimpan kompatibel dengan format live site (#, ##, **bold**, [IMG: url]).", "TRUE", ""],
  [21, "Customization urutan submenu pada navbar", "urutan submenu otomatis berdasarkan DEFAULT_NAV_ORDER_BY_SLUG (Macro Outlooks → Policy & Monetary → Geopolitical; Deep Dives → Regional → ESG → Commodity). client tidak perlu set navOrder manual — sistem mengisi otomatis saat load data dan render navbar.", "TRUE", ""],
  [22, "Submenu Commodity di Sectoral Intelligence", "submenu Commodity/Komoditas ditambahkan di Sectoral Intelligence dengan route /sectoral/commodity dan halaman CommodityPage.", "TRUE", ""],
];

const ws = XLSX.utils.aoa_to_sheet([
  ["No.", "Bug/Error", "Keterangan", "Status", "Catatan"],
  ...rows,
]);

ws["!cols"] = [{ wch: 5 }, { wch: 55 }, { wch: 90 }, { wch: 8 }, { wch: 45 }];

const wb = XLSX.utils.book_new();
XLSX.utils.book_append_sheet(wb, ws, "Bug Error");

const out = path.resolve(__dirname, "..", "..", "List Bug_Error_Complete.xlsx");
XLSX.writeFile(wb, out);
console.log("Written:", out);
