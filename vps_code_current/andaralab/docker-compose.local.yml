services:
  frontend:
    ports:
      - "8082:80"
    depends_on:
      - backend

  backend:
    ports:
      - "3003:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
      - DATA_DIR=/data
    volumes:
      - ../../fresh_production_RIGHT_NOW:/data
