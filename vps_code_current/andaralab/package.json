{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/enforce-pnpm.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "verify:cms": "node scripts/verify-cms-publish.mjs",
    "verify:contract": "node scripts/verify-deploy-contract.mjs",
    "hooks:install": "node scripts/install-git-hooks.mjs",
    "typecheck:artifacts": "pnpm -r --filter \"./artifacts/**\" --if-present run typecheck",
    "ci:predeploy": "pnpm run verify:contract && pnpm run typecheck:artifacts && pnpm --filter @workspace/api-server run build && pnpm run verify:cms && pnpm --filter @workspace/andaralab run build"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.1",
    "@tailwindcss/oxide-win32-x64-msvc": "^4.2.2",
    "lightningcss": "^1.32.0",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
